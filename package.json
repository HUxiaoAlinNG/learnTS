{
  "name": "learnts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run": "yarn compile && node ./src/type.js",
    "compile": "tsc -p .",
    "init": "tsc --init",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}